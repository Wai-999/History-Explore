---
import Layout from "../layouts/Layout.astro";
import Hero from "../components/Hero.astro";
import SectionHeading from "../components/SectionHeading.astro";
import countries from "../data/countries.json";
import usStates from "../data/usStates.json";
import cities from "../data/cities.json";

const sortedCountries = [...countries].sort((a, b) => b.stores - a.stores);
const sortedStates = [...usStates.states].sort((a, b) => b.stores - a.stores);
---
<Layout title="Global Presence" active="global">
  <Hero
    kicker="Section 04"
    title="A familiar map, 71 markets, drill-down to state and city"
    description="Pan and zoom like any map you already know. Click a pin (or use the dropdown) to open a market's profile — store count, ownership model, and, where the data exists, the states or cities behind the number."
  />

  <section class="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8">
    <div class="mb-6 flex flex-wrap items-center gap-4">
      <label class="flex-1 min-w-[220px]">
        <span class="mb-1 block text-xs font-semibold uppercase tracking-wide text-green-700">Jump to a market</span>
        <select id="country-select" class="w-full rounded-lg border border-green-900/20 bg-white px-3 py-2 text-sm">
          <option value="">Select a country…</option>
        </select>
      </label>
      <label class="flex items-center gap-2 text-sm font-medium text-espresso">
        <input type="checkbox" id="toggle-roasteries" checked class="h-4 w-4 rounded accent-green-700" />
        Reserve Roasteries
      </label>
      <label class="flex items-center gap-2 text-sm font-medium text-espresso">
        <input type="checkbox" id="toggle-cluster" checked class="h-4 w-4 rounded accent-green-700" />
        Cluster nearby markets
      </label>
      <button id="reset-view" type="button" class="rounded-full border border-green-900/20 bg-white px-4 py-2 text-sm font-semibold text-green-800 transition hover:bg-green-100">
        Reset view
      </button>
    </div>

    <div class="grid gap-6 lg:grid-cols-[1fr_360px]">
      <div class="card relative h-[520px] overflow-hidden sm:h-[620px]">
        <div id="map-canvas" class="h-full w-full"></div>
        <div class="pointer-events-none absolute bottom-3 left-3 z-[1000] flex flex-wrap gap-2 text-[11px]">
          <span class="rounded-full bg-white/90 px-2.5 py-1 text-espresso shadow-sm backdrop-blur">
            <span class="mr-1 inline-block h-2 w-2 rounded-full bg-[#0f4f3d] align-middle"></span>Reported
          </span>
          <span class="rounded-full bg-white/90 px-2.5 py-1 text-espresso shadow-sm backdrop-blur">
            <span class="mr-1 inline-block h-2 w-2 rounded-full bg-[#5c9c7c] align-middle"></span>Estimated
          </span>
          <span class="rounded-full bg-white/90 px-2.5 py-1 text-espresso shadow-sm backdrop-blur">
            <span class="mr-1 inline-block h-2 w-2 rounded-full bg-[#c6a664] align-middle"></span>Reserve Roastery
          </span>
        </div>
      </div>

      <aside id="country-panel" class="card h-fit p-6 lg:sticky lg:top-20">
        <p class="text-sm text-espresso-soft">Loading…</p>
      </aside>
    </div>

    <p class="mt-4 text-xs text-espresso-soft/70">
      Pins cluster into a numbered badge (showing combined store count) when markets are close together on screen —
      zoom in or click a cluster to split it apart. Drag to pan, scroll or use the +/− control to zoom, click any
      pin for its full profile.
    </p>
  </section>

  <script src="../scripts/map-init.ts"></script>

  <section id="us-states-table" class="border-t border-green-900/10 bg-green-100/50 scroll-mt-20">
    <div class="mx-auto max-w-5xl px-4 py-16 sm:px-6 lg:px-8">
      <SectionHeading num="A" title="United States, all 50 states + D.C." description={`Top 10 states account for ${usStates.top10Share} of the U.S. total. Six are directly reported by Starbucks/retail-analytics; the rest are population-weighted estimates scaled so the full table adds up to the reported national total.`} />
      <div class="card overflow-hidden">
        <div class="max-h-[520px] overflow-y-auto">
          <table class="w-full text-sm">
            <thead class="sticky top-0 bg-green-900 text-left text-cream">
              <tr>
                <th class="px-4 py-3 font-semibold">#</th>
                <th class="px-4 py-3 font-semibold">State</th>
                <th class="px-4 py-3 font-semibold text-right">Stores</th>
                <th class="px-4 py-3 font-semibold">Confidence</th>
              </tr>
            </thead>
            <tbody>
              {sortedStates.map((s, i) => (
                <tr class="border-b border-green-900/10 last:border-0 even:bg-green-50/50">
                  <td class="px-4 py-2.5 text-espresso-soft">{i + 1}</td>
                  <td class="px-4 py-2.5 font-medium">{s.name}</td>
                  <td class="px-4 py-2.5 text-right font-display">{s.stores.toLocaleString()}</td>
                  <td class="px-4 py-2.5">
                    <span class={`badge ${s.confidence === "reported" ? "badge-reported" : "badge-estimated"}`}>{s.confidence}</span>
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </section>

  <section class="mx-auto max-w-5xl px-4 py-16 sm:px-6 lg:px-8">
    <SectionHeading num="B" title="Notable cities & Reserve Roasteries" description="Starbucks doesn't publish city-level counts anywhere — these are the best publicly reported estimates, plus seven flagship locations: the original 1971 Pike Place store and six Reserve Roasteries (one, the original Capitol Hill Roastery, has since closed)." />
    <div class="grid gap-6 sm:grid-cols-2">
      <div class="card p-6">
        <h3 class="font-display text-lg font-semibold text-espresso">Most Starbucks-saturated cities (estimated)</h3>
        <ul class="mt-3 space-y-2">
          {cities.mostSaturatedCities.map((c) => (
            <li class="flex items-center justify-between border-b border-green-900/10 py-1.5 text-sm last:border-0">
              <span>{c.city}, <span class="text-espresso-soft">{c.country}</span></span>
              <span class="font-display font-semibold text-green-800">{c.stores.toLocaleString()}</span>
            </li>
          ))}
        </ul>
      </div>
      <div class="card p-6">
        <h3 class="font-display text-lg font-semibold text-espresso">Reserve Roasteries (flagship stores)</h3>
        <ul class="mt-3 space-y-2">
          {cities.flagshipRoasteries.map((r) => (
            <li class="border-b border-green-900/10 py-1.5 text-sm last:border-0">
              <div class="flex items-center justify-between">
                <span class="font-medium">{r.city}, {r.country}</span>
                <span class="text-espresso-soft">{r.opened}</span>
              </div>
              <p class="text-xs text-espresso-soft/80">{r.type}</p>
            </li>
          ))}
        </ul>
      </div>
    </div>
  </section>

  <section class="border-t border-green-900/10 bg-green-100/50">
    <div class="mx-auto max-w-6xl px-4 py-16 sm:px-6 lg:px-8">
      <SectionHeading num="C" title="Full country table" description="All 71 markets tracked on this map, sorted by store count. Starbucks discloses roughly 89 international markets in total — the ~18 not individually listed here are very small (a handful of stores each, often a single airport or hotel location) and get folded into Starbucks' own 'rest of world' reporting bucket rather than broken out. The six largest markets below are shown with sourced figures; the rest reflect Starbucks' broader licensed network at varying confidence." />
      <div class="card overflow-hidden">
        <div class="max-h-[600px] overflow-y-auto">
          <table class="w-full text-sm">
            <thead class="sticky top-0 bg-green-900 text-left text-cream">
              <tr>
                <th class="px-4 py-3 font-semibold">Country</th>
                <th class="px-4 py-3 font-semibold">Region</th>
                <th class="px-4 py-3 font-semibold text-right">Stores</th>
                <th class="px-4 py-3 font-semibold">Model</th>
                <th class="px-4 py-3 font-semibold">Confidence</th>
              </tr>
            </thead>
            <tbody>
              {sortedCountries.map((c) => (
                <tr class="border-b border-green-900/10 last:border-0 even:bg-green-50/50">
                  <td class="px-4 py-2.5 font-medium">{c.name}</td>
                  <td class="px-4 py-2.5 text-espresso-soft">{c.region}</td>
                  <td class="px-4 py-2.5 text-right font-display">{c.stores.toLocaleString()}</td>
                  <td class="px-4 py-2.5 text-espresso-soft">{c.model}</td>
                  <td class="px-4 py-2.5"><span class={`badge ${c.confidence === "reported" ? "badge-reported" : "badge-estimated"}`}>{c.confidence}</span></td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </section>
</Layout>
